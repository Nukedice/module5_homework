/*Задание 6.
Дан массив. Проверить, одинаковые ли элементы в массиве и вывести результат true или false в консоль. Речь идёт не о двух рядом стоящих одинаковых элементах, а обо всех. Проверить, все ли элементы в массиве одинаковые.*/

let arr1 = ['a', 'b', 'c', 'd', 'f', 'b', 'c', 'd']
let condt = true
for (let i = 0; i < arr1.length-1; i++) {
    if (arr1[i] == arr1[i+1]) {
           condt = false
    }
}
console.log (condt)
//Нужно задать 2 переменных, сравнить их ключи, и если они не совпадают, то сравнить их значение. Сделать это у меня не получилось (получался бесконечный цикл и браузер  с консолью зависал) - поэтому я взял неправильный ответ от Вашего ментора в Slaсk, который сравнивает соседние элементы и всегда выдаёт true :( Извините.